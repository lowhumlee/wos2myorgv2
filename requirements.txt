# WoS → MyOrg v2 — dependencies
# Python 3.9+ required

streamlit>=1.32.0        # Web UI
pandas>=1.5.0            # DataFrames and CSV handling
requests>=2.28.0         # MyOrg REST API calls

# Optional: faster fuzzy matching (drop-in, no code changes needed)
# rapidfuzz>=3.0.0
